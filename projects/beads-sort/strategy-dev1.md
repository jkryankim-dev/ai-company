# dev_1 전략 보고 — Beads Sort 기술 리스크 3 (경영전략회의)

작성: 이빌드 · 2026-08-19. 지시: inbox/pending/001-경영전략회의-beads-sort.md

**전제(사규 12조)**: 제품 저장소 `C:\dev\hanoi`는 대표 PC 경로며 이 VPS에는 존재하지 않는다(전역 find 실측, 사본 없음).
아래 회사 저장소 경로는 내가 직접 열어 확인했다. 제품 저장소 파일 경로는 그 문서에 기록된 실측값 인용임을 밝힌다.

**1. 안정성 — 재배포 빌드가 서명·크래시에 무방비하다**
- 키 없는 환경(CI·클론)에서 릴리즈 빌드가 **debug 서명으로 폴백**한다. 잘못 올린 AAB는 업로드 즉시 사고다 (`projects/beads-sort/context.md` '알려진 기술 결정').
- R8 축소 빌드는 실기기 실행 즉시 크래시 → v1 비활성 확정, 재활성 이슈 금지 (context.md, `decisions/0003`). 사전 출시 보고서 크래시 방치는 **단독 거절 사유** (`projects/beads-sort/sprint-01.md` 목표 A).
- 할 일: 재배포 체크리스트 1번 항목 = `android/key.properties` 존재 + 릴리즈 서명 확인. 빌드마다 `test-journal.md` 기록, 크래시 보고는 24시간 내 이슈화.

**2. 성능 — 성능·기기 호환 실측 기록 자체가 없다**
- 보유 테스트는 규칙 로직 2종뿐: `test/rules_test.dart`, `golden_pegs_test.dart` (context.md '이미 되어 있는 것'). integration_test·기기 매트릭스·프레임/시작시간 측정 기록 없음.
- 성능 문제가 있다고도 없다고도 단정할 수 없다 — **미측정이라는 사실이 리스크다**. 측정 전까지 출시 후 부정적 리뷰(느림·특정 기기 크래시)를 대응할 근거가 없다.
- 할 일: 옵트인 테스터 단말 상위 기종에서 수동 스모크 후 저널 기록. v1.1에 integration_test 도입 신규 이슈 제안.

**3. 정책 — 거절 직결 미확인 3종이 빌드 설정에 묻혀 있다**
- targetSdk 요건 충족 미확인(flutter 버전에 종속) — `android/app/build.gradle` 실측 필요 (context.md '확인이 필요한 것').
- AD_ID 권한 선언 미확인(`google_mobile_ads ^9.1.0` 매니페스트 병합) + 데이터 안전 양식 미제출 (context.md 동일 섹션).
- 이슈 #5(박빌드)·#6(이빌드)가 이미 담당 분야로 존재 — 본 보고서는 중복 이슈를 만들지 않는다.

**대표 승인 필요 — 제품 저장소 실측 경로 확보.** #5·#6의 '실측' 단계가 VPS에서 저장소를 읽을 수 없어 진행 불가다.
- 선택지 A: 대표가 PC에서 측정값(파일 발췌)을 전달 — 저장소 정책 불변, 주고받는 데 대표 시간 소요.
- 선택지 B: 제품 저장소를 회사 org에 미러링해 VPS가 읽게 함 — 전 직원이 사규 12조로 직접 검증 가능, 저장소 공개 범위 결정이 선행.
- 추천 B — 실측 없는 보고가 누적되면 모든 부서 보고서의 근거 품질이 내려간다.
